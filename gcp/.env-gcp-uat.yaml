GOOGLE_CLOUD_PROJECT: ib-product-application-uat
FIRESTORE_DATABASE_ID: support-portal-faqs
# Public site + the private admin portal (ib-product-support-portal-admin repo).
# The admin app is a separate origin, so it must be allowed here for CORS.
ALLOWED_ORIGINS: "https://uat-support.indiabullssecurities.com,https://uat-support-admin.indiabullssecurities.com,https://uat-support-masteradmin.indiabullssecurities.com,https://ib-admin-uat.web.app,https://ib-admin-uat.firebaseapp.com"
# ── Email-OTP login (non-secret config; SMTP_USER/SMTP_PASS come from Secret Manager) ──
OTP_ALLOWED_DOMAIN: "indiabulls.com"
# Google Workspace SSO — paste your OAuth Web client ID (non-secret; also used
# by the browser). Get it from GCP Console → APIs & Services → Credentials.
GOOGLE_CLIENT_ID: "620876318042-3768hqa8n87ip4uve69r70t4lt751l0l.apps.googleusercontent.com"
OTP_FROM_ADDR: "no-reply@indiabulls.com"
OTP_SEED_MASTERS: "sumit.bagewadi@indiabulls.com"
# Govern-after self-signup: any verified @indiabulls.com Google account with no
# record becomes an active manager on first login (master admin can then
# deactivate/delete). Master role is never auto-granted.
AUTO_PROVISION_MANAGERS: "true"
SMTP_PORT: "587"
# SMTP_HOST is non-secret; set it to your corporate relay hostname before deploy.
SMTP_HOST: ""
